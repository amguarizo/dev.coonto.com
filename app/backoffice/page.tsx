import { requireUser } from "@/lib/auth";
import { SiteHeader } from "@/components/site-header";
import { query } from "@/lib/db";

export const dynamic="force-dynamic";
export default async function Backoffice(){const user=await requireUser("/backoffice");if(user.role!=="admin")return <main className="page"><SiteHeader/><div className="content page-hero"><span className="section-kicker">ÁREA RESTRITA</span><h1>Este painel pertence à operação Coonto.</h1><p>Sua conta não possui acesso administrativo.</p></div></main>;const counts=await query<{members:string;starts:string;completed:string;feedbacks:string;partners:string}>(`SELECT (SELECT COUNT(*) FROM users)::text AS members,(SELECT COUNT(*) FROM learning_progress)::text AS starts,(SELECT COUNT(*) FROM learning_progress WHERE completed=TRUE)::text AS completed,(SELECT COUNT(*) FROM feedback)::text AS feedbacks,(SELECT COUNT(*) FROM partner_leads)::text AS partners`);const c=counts.rows[0];const metrics=[["Membros cadastrados",c.members],["Experiências iniciadas",c.starts],["Experiências concluídas",c.completed],["Feedbacks recebidos",c.feedbacks]];return <main className="backoffice"><SiteHeader/><div className="content"><section className="backoffice-hero"><span className="section-kicker">BACKOFFICE · DADOS REAIS DA VALIDAÇÃO</span><h1>O que aprendemos antes de cobrar.</h1><p>As métricas abaixo são alimentadas pelo uso da plataforma publicada. Parceiros interessados: <strong>{c.partners}</strong>.</p></section><section className="metrics">{metrics.map(([label,value])=><div className="metric" key={label}><span>{label}</span><strong>{value}</strong></div>)}</section><section className="dashboard-grid"><article className="dashboard-card"><h2>Jornada mensurável</h2><div className="architecture"><div>1. Aquisição<span>Origem, perfil e interesse do visitante.</span></div><div>2. Ativação<span>Conta criada e primeira obra aberta.</span></div><div>3. Compreensão<span>Decisões, progresso e conclusão.</span></div><div>4. Retenção<span>Retomada, catálogo e Coonto Club.</span></div></div></article><article className="dashboard-card"><h2>Assinaturas · arquitetura Stripe</h2><div className="architecture"><div>1. Checkout Session<span>Compra avulsa ou assinatura recorrente.</span></div><div>2. Webhook verificado<span>Atualiza pagamento e situação da assinatura.</span></div><div>3. Base Coonto<span>Relaciona membro, acesso, obra e histórico.</span></div><div>4. Customer Portal<span>Cliente gerencia pagamento e cancelamento.</span></div></div><p><span className="status-pill">PRONTO PARA INTEGRAÇÃO</span> Cobranças ainda não estão ativas.</p></article><article className="dashboard-card"><h2>Proteção de acesso</h2><div className="architecture"><div>Conta e permissão<span>A obra não circula como arquivo público.</span></div><div>Dois aparelhos<span>Cada membro controla seus dispositivos.</span></div><div>Licença de 30 dias<span>Uso offline com renovação periódica.</span></div><div>Histórico na plataforma<span>O valor acompanha a conta, não uma cópia.</span></div></div></article><article className="dashboard-card"><h2>O que já está operacional</h2><div className="architecture"><div>Catálogo e experiência<span>Entrada pública e obra dentro da conta.</span></div><div>Progresso e retomada<span>Sincronização entre sessões e aparelhos.</span></div><div>Feedback e parceiros<span>Dados persistentes para a validação.</span></div><div>PWA instalável<span>Web, mobile e modo offline controlado.</span></div></div></article></section></div></main>;}

